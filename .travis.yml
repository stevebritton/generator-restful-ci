language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-codeigniter' ]; then cd .. && eval "mv $currentfolder generator-codeigniter" && cd generator-codeigniter; fi

